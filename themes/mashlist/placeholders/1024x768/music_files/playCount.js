jQuery.songPlayCount = (function(options){
	var defaults = {};
	var options = $.extend(defaults,options);
	
	function incrementPlayCount() {
		// increments the play counter on a song
	}
	
	function getPlayCount() {
		// retrieves the play counter
	}

});
