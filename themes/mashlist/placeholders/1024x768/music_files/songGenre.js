jQuery.songGenre = (function(options){
	var defaults = {};
	var options = $.extend(defaults,options);
	
	function addSongGenre() {
		// send songId, genreId via ajax
	}
	
	function removeSongGenre() {
		// send songGenreId via ajax
	}

});
