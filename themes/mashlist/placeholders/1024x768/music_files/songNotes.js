jQuery.songNotes = (function(options){
	var defaults = {};
	var options = $.extend(defaults,options);
	
	function saveNote() {
		// sends note info via ajax
	}
	
	function hasNote() {
		// checks if a song has a?? note
	}
	
});
