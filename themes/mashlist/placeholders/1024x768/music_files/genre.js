jQuery.genre = (function(options){
	var defaults = {};
	var options = $.extend(defaults,options);
	
	function create() {
		// send a genre name via ajax
	}
	
	function update() {
		// updates a genre name via ajax
	}
	
});
